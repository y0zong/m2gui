use std::any;

pub struct App{
    // language: any,
    // backend
    // event
}

impl App {
    fn build(){
        todo!()
    }

    fn exit() {
        todo!()
    }

    fn run(mut self){
        let mut runner = std::mem.REPLACE(&mut self.runner, Box::new())
    }
}